# Autenticacao por arquivo de senhas (bcrypt). Gerar com:
#   htpasswd -B -C 10 -c trino/config/password.db delfos_user
# Arquivo NAO versionado (ver .gitignore).
password-authenticator.name=file
file.password-file=/etc/trino/password.db
